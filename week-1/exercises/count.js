let count = 0;

count = count + 1;

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing

/*  In line 3, count = count + 1; updates the count variable. It takes the current value of count (0), adds 1, and assigns the result (1) back to count. After this line, count holds the value 1. */