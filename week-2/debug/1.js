// Predict and explain first...


/* The sum function has return on its own line, which causes JavaScript to automatically insert a semicolon, ending the function there and returning undefined */

function sum(a, b) {
  return  a + b;
 
}

console.log(`The sum of 10 and 32 is ${sum(10, 32)}`);
